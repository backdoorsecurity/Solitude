[Unit]
Description=Xpra persistent server for isolated Falkon browser
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/xpra start
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
